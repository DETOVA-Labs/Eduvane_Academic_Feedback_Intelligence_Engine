# Eduvane Web Workspace

React/TypeScript chat-first frontend:

- Sidebar conversation history
- Bottom-anchored chat composer
- Upload control integrated into composer
- Soft thinking indicator
- No automatic first message

## Run

1. Copy `.env.example` to `.env`.
2. `npm install`
3. `npm run dev`
